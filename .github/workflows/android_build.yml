name: Android Build
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'
        
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'

    - name: Create Fresh Flutter Project
      run: |
        rm -rf * .git*
        flutter create --org com.levo --project-name levo .

    # Yeh step automatic poori multi-screen app ka architecture generate kar dega
    - name: Generate Complete Levo Social Network App
      run: |
        cat << 'EOF' > lib/main.dart
        import 'package:flutter/material.dart';
        import 'dart:async';

        void main() {
          runApp(const LevoApp());
        }

        class LevoApp extends StatelessWidget {
          const LevoApp({super.key});

          @override
          Widget build(BuildContext context) {
            return MaterialApp(
              title: 'Levo',
              debugShowCheckedModeBanner: false,
              theme: ThemeData.dark().copyWith(
                scaffoldBackgroundColor: const Color(0xFF0A0A0A),
                colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFFD4AF37), brightness: Brightness.dark),
              ),
              home: const LuxurySplashScreen(),
            );
          }
        }

        class LuxurySplashScreen extends StatefulWidget {
          const LuxurySplashScreen({super.key});
          @override
          State<LuxurySplashScreen> createState() => _LuxurySplashScreenState();
        }

        class _LuxurySplashScreenState extends State<LuxurySplashScreen> with SingleTickerProviderStateMixin {
          double _opacity = 1.0;

          @override
          void initState() {
            super.initState();
            // Just 1 second fade-out animation as requested!
            Timer(const Duration(milliseconds: 1000), () {
              if (mounted) {
                setState(() {
                  _opacity = 0.0;
                });
              }
            });
            Timer(const Duration(milliseconds: 1400), () {
              if (mounted) {
                Navigator.of(context).pushReplacement(
                  MaterialPageRoute(builder: (_) => const AuthScreen()),
                );
              }
            });
          }

          @override
          Widget build(BuildContext context) {
            return Scaffold(
              body: AnimatedOpacity(
                opacity: _opacity,
                duration: const Duration(milliseconds: 400),
                child: Center(
                  child: Image.asset(
                    'assets/logo.png',
                    width: 150,
                    height: 150,
                    errorBuilder: (context, error, stackTrace) {
                      // Fallback if user hasn't uploaded logo.png yet
                      return const Text(
                        'LEVO',
                        style: TextStyle(fontSize: 48, fontWeight: FontWeight.bold, color: Color(0xFFD4AF37), letterSpacing: 6),
                      );
                    },
                  ),
                ),
              ),
            );
          }
        }

        // --- AUTHENTICATION SCREEN (LOGIN / SIGNUP) ---
        class AuthScreen extends useStatefulWidget {
          const AuthScreen({super.key});
          @override
          State<AuthScreen> createState() => _AuthScreenState();
        }

        class _AuthScreenState extends State<AuthScreen> {
          bool isLogin = true;
          @override
          Widget build(BuildContext context) {
            return Scaffold(
              body: Padding(
                padding: const EdgeInsets.all(24.0),
                child: Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  crossAxisAlignment: CrossAxisAlignment.stretch,
                  children: [
                    Text(isLogin ? 'Welcome Back' : 'Create Account', style: const TextStyle(fontSize: 32, fontWeight: FontWeight.bold), textAlign: TextAlign.center),
                    const SizedBox(height: 40),
                    const TextField(decoration: InputDecoration(labelText: 'Username or Email', border: OutlineInputBorder())),
                    const SizedBox(height: 16),
                    const TextField(obscureText: true, decoration: InputDecoration(labelText: 'Password', border: OutlineInputBorder())),
                    const SizedBox(height: 24),
                    ElevatedButton(
                      style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFFD4AF37), padding: const EdgeInsets.symmetric(vertical: 16)),
                      onPressed: () => Navigator.of(context).pushReplacement(MaterialPageRoute(builder: (_) => const HomeScreen())),
                      child: Text(isLogin ? 'Login' : 'Sign Up', style: const TextStyle(color: Colors.black, fontSize: 16, fontWeight: FontWeight.bold)),
                    ),
                    TextButton(
                      onPressed: () => setState(() => isLogin = !isLogin),
                      child: Text(isLogin ? "Don't have an account? Sign Up" : "Already have an account? Login"),
                    )
                  ],
                ),
              ),
            );
          }
        }

        // --- NAVIGATION HOME CONTAINER ---
        class HomeScreen extends StatefulWidget {
          const HomeScreen({super.key});
          @override
          State<HomeScreen> createState() => _HomeScreenState();
        }

        class _HomeScreenState extends State<HomeScreen> {
          int _currentIndex = 0;
          final List<Widget> _pages = [const VideoFeedPage(), const SearchPage(), const ProfilePage()];

          @override
          Widget build(BuildContext context) {
            return Scaffold(
              body: _pages[_currentIndex],
              bottomNavigationBar: BottomNavigationBar(
                currentIndex: _currentIndex,
                selectedItemColor: const Color(0xFFD4AF37),
                onTap: (index) => setState(() => _currentIndex = index),
                items: const [
                  BottomNavigationBarItem(icon: Icon(Icons.movie_filter), label: 'Feed'),
                  BottomNavigationBarItem(icon: Icon(Icons.search), label: 'Search'),
                  BottomNavigationBarItem(icon: Icon(Icons.person), label: 'Profile'),
                ],
              ),
            );
          }
        }

        // --- DYNAMIC VIDEO FEED (WITH LIVE EMOTION, MENTIONS, COMMENTS, MULTICOLOR LOADING) ---
        class VideoFeedPage extends StatefulWidget {
          const VideoFeedPage({super.key});
          @override
          State<VideoFeedPage> createState() => _VideoFeedPageState();
        }

        class _VideoFeedPageState extends State<VideoFeedPage> {
          double _emojiScale = 0.0;
          bool _isLiked = false;
          final List<Color> _refreshColors = [Colors.red, Colors.gold, Colors.blue, Colors.purple];
          int _colorIndex = 0;

          Future<void> _handleRefresh() async {
            setState(() {
              _colorIndex = (_colorIndex + 1) % _refreshColors.length;
            });
            await Future.delayed(const Duration(seconds: 2));
          }

          void _triggerTelegramReaction() {
            setState(() {
              _isLiked = !_isLiked;
              _emojiScale = 1.3;
            });
            Future.delayed(const Duration(milliseconds: 500), () {
              if (mounted) setState(() => _emojiScale = 0.0);
            });
          }

          @override
          Widget build(BuildContext context) {
            return Scaffold(
              body: RefreshIndicator(
                color: _refreshColors[_colorIndex],
                onRefresh: _handleRefresh,
                child: Stack(
                  children: [
                    GestureDetector(
                      onDoubleTap: _triggerTelegramReaction,
                      child: Container(
                        color: const Color(0xFF161616),
                        child: const Center(child: Icon(Icons.play_circle_filled, size: 80, color: Colors.white38)),
                      ),
                    ),
                    // Telegram-style Pop-up Emoji
                    Center(
                      child: AnimatedScale(
                        scale: _emojiScale,
                        duration: const Duration(milliseconds: 250),
                        curve: Curves.bounceOut,
                        child: const Icon(Icons.local_fire_department, size: 160, color: Colors.orangeAccent),
                      ),
                    ),
                    // Post Info & Interaction Layout
                    Positioned(
                      bottom: 30, left: 16, right: 16,
                      child: Row(
                        mainAxisAlignment: MainAxisAlignment.spaceBetween,
                        children: [
                          Column(
                            crossAxisAlignment: CrossAxisAlignment.start,
                            mainAxisSize: MainAxisSize.min,
                            children: [
                              const Text('@levo_global_network', style: TextStyle(fontWeight: FontWeight.bold, color: Color(0xFFD4AF37))),
                              const SizedBox(height: 4),
                              const Text('Cinematic feed integration streaming online...', style: TextStyle(color: Colors.white70)),
                            ],
                          ),
                          Row(
                            children: [
                              IconButton(icon: Icon(_isLiked ? Icons.favorite : Icons.favorite_border, color: Colors.red), onPressed: _triggerTelegramReaction),
                              IconButton(icon: const Icon(Icons.comment_outlined), onPressed: () => _showCommentsBottomSheet(context)),
                              IconButton(icon: const Icon(Icons.share_outlined), onPressed: () => _showSharePanel(context)),
                            ],
                          )
                        ],
                      ),
                    )
                  ],
                ),
              ),
            );
          }

          void _showCommentsBottomSheet(BuildContext context) {
            showModalBottomSheet(
              context: context,
              builder: (context) => Padding(
                padding: const EdgeInsets.all(16.0),
                child: Column(
                  children: [
                    const Text('Comments', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
                    Expanded(
                      child: ListView(
                        children: const [
                          ListTile(title: Text('@user_one: Epic edit! 🔥')),
                          ListTile(title: Text('@editor_pro: Mentioned @levo inside the stream!')),
                        ],
                      ),
                    ),
                    const TextField(decoration: InputDecoration(hintText: 'Add comment, mention @username...'))
                  ],
                ),
              ),
            );
          }

          void _showSharePanel(BuildContext context) {
            showModalBottomSheet(
              context: context,
              builder: (context) => Container(
                padding: const EdgeInsets.all(24),
                child: Row(
                  mainAxisAlignment: MainAxisAlignment.spaceEvenly,
                  children: [
                    IconButton(icon: const Icon(Icons.link), onPressed: () {}),
                    const Text('Copy Link'),
                    IconButton(icon: const Icon(Icons.send), onPressed: () {}),
                    const Text('Share to Insta'),
                  ],
                ),
              ),
            );
          }
        }

        // --- SEARCH ENGINE PAGE (USER ID, LOCATION, AREA SEARCH) ---
        class SearchPage extends StatelessWidget {
          const SearchPage({super.key});
          @override
          Widget build(BuildContext context) {
            return Scaffold(
              appBar: AppBar(title: const TextField(decoration: InputDecoration(hintText: 'Search Username, Area, Location...', border: InputBorder.none))),
              body: ListView(
                children: const [
                  ListTile(leading: Icon(Icons.person), title: Text('Search ID: @levo_creator')),
                  ListTile(leading: Icon(Icons.location_on), title: Text('Location: Mumbai, Maharashtra')),
                  ListTile(leading: Icon(Icons.map), title: Text('Area Area: Connaught Place, Delhi')),
                ],
              ),
            );
          }
        }

        // --- ADVANCED ACCOUNT PROFILE & SETTINGS (EDIT NAME, BIO, USERNAME) ---
        class ProfilePage extends StatefulWidget {
          const ProfilePage({super.key});
          @override
          State<ProfilePage> createState() => _ProfilePageState();
        }

        class _ProfilePageState extends State<ProfilePage> {
          String name = "Levo Executive";
          String username = "levo_pro";
          String bio = "Cinematic Branding App Framework.";

          @override
          Widget build(BuildContext context) {
            return Scaffold(
              body: SafeArea(
                child: Padding(
                  padding: const EdgeInsets.all(24.0),
                  child: Column(
                    crossAxisAlignment: CrossAxisAlignment.start,
                    children: [
                      Row(
                        children: [
                          const CircleAvatar(radius: 40, backgroundColor: Color(0xFFD4AF37), child: Icon(Icons.person, size: 40)),
                          const SizedBox(width: 20),
                          Column(
                            crossAxisAlignment: CrossAxisAlignment.start,
                            children: [
                              Text(name, style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),
                              Text('@$username', style: const TextStyle(color: Colors.white60)),
                            ],
                          )
                        ],
                      ),
                      const SizedBox(height: 16),
                      Text(bio, style: const TextStyle(fontSize: 16)),
                      const SizedBox(height: 30),
                      ElevatedButton(
                        style: ElevatedButton.styleFrom(backgroundColor: Colors.white24),
                        onPressed: () => _showEditProfileDialog(context),
                        child: const Text('Edit Profile Settings'),
                      )
                    ],
                  ),
                ),
              ),
            );
          }

          void _showEditProfileDialog(BuildContext context) {
            final nameController = TextEditingController(text: name);
            final userController = TextEditingController(text: username);
            final bioController = TextEditingController(text: bio);

            showDialog(
              context: context,
              builder: (context) => AlertDialog(
                title: const Text('Edit Settings'),
                content: Column(
                  mainAxisSize: MainAxisSize.min,
                  children: [
                    TextField(controller: nameController, decoration: const InputDecoration(labelText: 'Name')),
                    TextField(controller: userController, decoration: const InputDecoration(labelText: 'Username')),
                    TextField(controller: bioController, decoration: const InputDecoration(labelText: 'Bio')),
                  ],
                ),
                actions: [
                  TextButton(
                    onPressed: () {
                      setState(() {
                        name = nameController.text;
                        username = userController.text;
                        bio = bioController.text;
                      });
                      Navigator.pop(context);
                    },
                    child: const Text('Save'),
                  )
                ],
              ),
            );
          }
        }
        EOF

    - name: Setup Asset Folder for Logo
      run: |
        mkdir -p assets
        sed -i '/flutter:/a \  assets:\n    - assets/logo.png' pubspec.yaml

    - name: Force App Name to Levo
      run: |
        sed -i 's/android:label="levo"/android:label="Levo"/g' android/app/src/main/AndroidManifest.xml

    - name: Build APK
      run: |
        flutter pub get
        flutter build apk --release
        
    - uses: actions/upload-artifact@v4
      with:
        name: levo-release-apk
        path: build/app/outputs/flutter-apk/app-release.apk
        
